# SPDX-FileCopyrightText: 2026 数链创元（天津）信息技术有限责任公司
# SPDX-License-Identifier: AGPL-3.0-or-later
name: Stability soak

on:
  schedule:
    - cron: "23 18 * * 6"
  workflow_dispatch:
    inputs:
      iterations:
        description: Parallel cancellation repetitions
        required: true
        default: "20"

permissions:
  contents: read

concurrency:
  group: stability-soak-${{ github.ref }}
  cancel-in-progress: false

jobs:
  bounded-soak:
    name: Connector and terminal soak
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      STABILITY_ITERATIONS: ${{ inputs.iterations || '20' }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7, pinned
        with:
          persist-credentials: false
      - name: Run bounded stability acceptance
        run: python tools/acceptance/test_stability_acceptance.py --profile soak --iterations "$STABILITY_ITERATIONS" --with-databases
      - name: Retain sanitized stability report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: stability-soak-${{ runner.os }}-${{ runner.arch }}
          path: dist/stability-acceptance.json
          if-no-files-found: warn
          retention-days: 30
