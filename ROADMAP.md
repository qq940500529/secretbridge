# SecretBridge 开源版路线图

[项目首页](README.zh-CN.md) / [文档中心](docs/README.md) / [版本差异](docs/开源版与企业版.md) / [成熟态架构](docs/成熟态软件架构与功能说明.md)

SecretBridge 当前公开仓库交付面向个人用户的本机开源版：用户在自己的电脑上保存凭据，AI 不读取凭据原文，但可以通过受控的本地代理完成真正的终端、数据库、SSH 和 HTTP 任务，并持续获得脱敏结果。

开源版是独立可用的公开发行产品，按照成熟开源软件的标准建设，但产品范围限定为单机、单用户和本地执行。团队协作、集中治理与规模化部署属于未来另行开发的闭源商业企业版，不作为开源版 1.0 的前置条件。具体边界见[开源版与企业版产品边界](docs/开源版与企业版.md)。

路线图从 `0.1.0-alpha.22` 起按“先可用、后完善、发布前集中加固”的顺序推进。安全是执行底座的约束，不扩展成面向普通用户的认证或证据管理产品。

## 开发原则

1. 每个版本交付一个可在界面和 AI 接口中端到端使用的大功能包。
2. 优先完成真实终端、凭据注入、任务模板和常用连接器，不用合成能力代替真实功能。
3. 必要安全控制随功能同时实现：秘密不回读、本机绑定、权限校验、输出脱敏、临时材料清理和失败状态。
4. 自动化泄漏检查、故障注入、依赖治理、SBOM 和发行物验证在功能基本完整后集中进行；代码签名、公证和软件源签名不作为开源版 1.0 的发布条件。
5. 开发阶段、里程碑编号和安全宣传不进入运行界面；界面只表达用户任务、状态和下一步操作。
6. 不通过“通用脚本加秘密”绕过产品边界。每种注入方式和连接器都必须明确输入、输出、生命周期与清理规则。
7. 开源版不为制造商业差异而削弱个人本机核心能力；企业版差异来自多人协作、集中治理、规模化部署和商业服务。

## 当前基线：0.2.0-beta.3

### 已保留的产品底座

- 本机 Web 控制台、一次性浏览器配对、可撤销页面会话和精确 Origin 校验；
- SQLite 配置目录与操作系统凭据库，秘密只有写入、更新和删除路径，没有读取或导出 API；
- 凭据引用、目标、操作模板、单次或限时范围授权、参数化运行和固定审计事件；
- 后台代理与 MCP stdio 桥接分离，使用 Windows 命名管道或 Unix domain socket 通信；
- 可重连的真实 PTY：Windows PowerShell／CMD、Linux Bash、macOS Zsh，支持名称、工作目录、普通环境、PID、退出码、输入租约、游标回放和输出背压；
- MCP 普通终端控制、独立会话输入权、长轮询增量读取，以及 Web 审批／任务／终端变更实时通知；
- 固定程序凭据任务、四种命名插槽注入方式、跨分片脱敏输出与 MCP 续读；
- PostgreSQL 固定只读连接检查，以及平台信任根或显式私有 CA 支持。
- 原生 HTTP/HTTPS 固定请求、协议认证插槽、普通参数、状态码与 JSON Pointer 字段筛选。
- 原生 SSH 固定命令、可信 SHA256 指纹、密码/私钥协议认证、逐项 POSIX 参数引用与持续脱敏输出。
- SFTP 固定单文件上传／下载、显式覆盖、原子提交与有界临时文件清理。
- Git HTTPS 令牌代用、固定分支远端检查、拉取与非强制推送。
- PostgreSQL/MySQL 原生连接检查、版本和登记的参数化只读查询，指定返回列、保留精度的表格结果与有界取消。
- 设置页非秘密配置追加迁移、导入预检与持久摘要幂等、一致性备份、全新目录恢复、版本迁移回滚副本和精简诊断。

### 本次删除的超前产品层

- 安全验证与证据中心；
- 平台身份与 IPC 证据工作区；
- 试点准入快照；
- 低权限试点活动、固定场景矩阵和相关报告；
- 上述能力对应的页面、API、领域模型、测试和数据库表。

旧版数据库升级到 schema v12 时会删除这些废弃表，同时保留凭据引用、目标、模板、审批、运行和审计数据。

## 近期开发：把核心工作流做完整

### 0.1.0-alpha.23 · 真实持久终端（已完成）

目标：用真正的系统终端替代当前合成 PTY，让用户可以在 Web 界面完成日常命令行工作。

- Windows 支持 PowerShell 和 CMD；Linux 支持 Bash；macOS 支持 Zsh；
- 创建、命名、列出、连接、断开、重连、调整大小和终止会话；
- 保持工作目录和普通环境状态，支持长任务与流式 stdout/stderr；
- 区分窗口断开、会话退出和强制终止；
- 支持输入租约，避免 Web 用户和 AI 同时写入；
- 验收：三个平台至少各完成一次真实 shell 会话，关闭浏览器后可重新连接，长输出与取消行为明确。

实现已包含平台能力探测、白名单Shell解析、Web创建表单、显式窗口断开、自动／手动重连、进程退出和强制终止语义，并由三平台CI运行真实Shell集成测试。测试专用合成PTY只用于稳定复现输出洪泛、游标截断和撤销场景，不再出现在正式界面。

### 0.1.0-alpha.24 · AI 终端控制与即时状态（已完成）

目标：让 AI 通过本机 MCP 操作真实终端，并及时获得增量结果。

- 新增终端列表、创建、连接、写入、增量读取、调整尺寸、取消和关闭工具；
- 输出使用单调游标续读，断线后不重复执行命令；
- WebSocket 向 Web 控制台实时推送会话、任务和审批变化；
- MCP 返回终端状态、Shell退出码和有界的普通进程输出，不返回内部认证令牌；普通终端不注入凭据，凭据输出脱敏由下一功能包提供；
- 验收：AI 可完成“创建终端—执行普通命令—持续读取—结束会话”的完整闭环。

实现包含10个终端工具，复用现有真实PTY和原生IPC；MCP客户端之间及Web页面通过输入租约协调。正常MCP断开释放输入权，异常退出的附件在60秒空闲期限后清理，终端进程保留。读取最多16KiB字节、最多等待5秒，提供单调游标、保留缺口、原始字节及UTF-8预览；写入失败不自动重试。Web变更流只推送失效通知，不承载资源值或终端输出，页面重连后重新获取快照，并保留低频兜底刷新。

2026-09-17，功能提交 `44ce15d` 已通过 Windows、Linux 和 macOS 的全套 CI，包含真实 Shell 的 MCP／原生 IPC 端到端验收、Web 测试、Rust 检查和依赖许可检查。实现见 [PR #25](https://github.com/qq940500529/secretbridge/pull/25)。

### 0.1.0-alpha.25 · 凭据注入与跨分片脱敏（已完成）

目标：让 AI 能使用凭据执行任务，同时不通过正常工具接口取得凭据原文。

- 在 UI 中把凭据绑定到任务模板的命名插槽；AI 只看到插槽名称和用途；
- 支持标准输入、临时环境变量、命令参数和短期配置文件四种注入方式；
- UI 明确显示每种方式的暴露风险，默认优先标准输入或目标工具的原生认证机制；
- 注入值不写入命令历史、任务定义、SQLite、普通终端环境或 MCP 请求；
- stdout、stderr、错误信息、审计摘要和实时分片使用同一跨分片脱敏器；
- 任务结束、取消和服务异常恢复时清除临时文件与可清理内存；
- 验收：使用合成秘密覆盖分片、编码、回显、取消和异常路径，AI 只获得脱敏结果。

已实现固定程序模板、最多8个命名凭据插槽、四种注入方式、显式脱敏输出范围、审批详情配置预览、单次授权与幂等运行、四进程并发限制、超时／取消、固定退出状态、临时文件清理与启动恢复清理。stdout/stderr在解码、保存和返回前执行字节级过滤，增量UTF-8解码避免中文跨分片损坏；Web和MCP共享有界分片游标输出。schema v13保存配置及已过滤输出，凭据轮换使关联模板版本递增。当前不提供交互式密码提示，也不承诺识别任意秘密变换或隔离恶意程序；详见[凭据命令任务](docs/凭据命令任务.md)。

2026-09-18，功能提交 `abb302f` 已通过 Windows、Linux 和 macOS 的全套 CI，包括四种注入方式的真实程序验收和原生 MCP／IPC 端到端读取。实现见 [PR #26](https://github.com/qq940500529/secretbridge/pull/26)。本地回归为 Rust 76 项通过、2 项显式跳过，Web 29 项通过，仓库工具 20 项通过；格式、静态检查、构建与锁定依赖许可检查均通过，未新增依赖。

### 0.1.0-alpha.26 · 可复用任务模板（已完成）

目标：把常用操作从手工拼命令升级为可保存、可审阅、可复用的任务。

- 模板包含固定程序、参数结构、工作目录、普通参数、凭据插槽、注入方式、超时和结果规则；
- 普通参数有类型、必填、默认值、枚举和长度约束；
- 支持“每次确认”“一段时间内允许”和“仅当前一次”三种用户授权方式；
- 模板变更后旧授权失效；重复请求使用幂等键避免重复执行；
- 任务详情展示实时输出、最终状态、退出码、耗时和可重试建议；
- 验收：从 UI 创建模板后，Web 与 MCP 均可使用同一执行链完成任务。

已实现文本／整数／布尔参数、必填／默认／枚举／长度校验、独立参数命名空间、审批参数快照、三种授权意图、限时相同参数复用、配置漂移失效、幂等去重、审批实参预览、运行耗时与重试建议。schema v14升级保留输出、退出码和事件；Web与MCP共用校验和执行器。当前每次确认与仅当前一次均使用单次消费规则，限时授权不允许修改参数。使用说明见[参数化任务与授权](docs/参数化任务与授权.md)。

本地验收：Rust 83项通过、2项显式跳过（系统凭据库写入及外部TLS数据库实测），Web 37项通过、仓库工具20项通过；Web与MCP分别完成参数化真实程序执行，覆盖默认值、输入拒绝、审批、限时复用、幂等、撤销、凭据过滤和v13升级。页面在启动结果不确定时保留幂等键，避免复用授权下的重复业务操作。构建、格式、静态分析和锁定依赖许可验证通过，未增加应用依赖。实现与三平台检查见[PR #27](https://github.com/qq940500529/secretbridge/pull/27)。

2026-09-19，PR #26、#27 已依次合并。同步主分支后的 Windows 检查发现运行完成状态与退出码之间存在可见竞态，已改为同一事务提交，并增加确定性的原子性回归测试；PR #27 最终提交通过三平台完整检查。

### 0.1.0-alpha.27 · 原生 HTTP 连接器（已完成本地验收）

目标：完整交付从界面配置到 AI 执行与结果读取的 HTTP 凭据代用工作流。

- 已实现固定方法与 URL、认证头与顶层 JSON 请求体凭据插槽、自动编码查询参数、保持类型的普通参数；
- 已接入模板版本、凭据轮换失效、审批快照、单次／限时授权、幂等去重、并发、超时、取消和 Web／MCP 输出；
- 已实现默认状态码返回、显式 JSON Pointer 字段筛选、256 KiB 响应上限、固定错误代码和保存前脱敏；
- HTTPS 使用平台信任根，不跟随重定向、不使用环境代理、不导出 TLS 密钥；
- 已新增真实本机接口和原生 MCP／IPC 验收；不包含上传、任意响应正文、动态路径、嵌套请求体或独立私有 CA 文件。

详细配置与边界见[HTTP 凭据任务](docs/HTTP凭据任务.md)。常用连接器按协议拆为可完整验收的版本，避免把尚未交付的 SSH、Git 或数据库查询算入本版本。

本地验收：Rust 92 项通过、2 项显式跳过，Web 45 项通过，仓库工具 20 项通过；格式、Clippy、类型检查、构建和锁定依赖许可检查通过（373 个 Rust 包、5 组 npm 许可）。Web API 与原生 MCP／IPC 均完成真实本机接口执行；覆盖参数类型、凭据回显、字段路径、响应上限、错误映射、幂等、限时重复、超时、取消及配置重开。终端洪泛回归按明确的背压通知重新连接，不再假定固定等待后必然发完。实现及三平台检查见[PR #28](https://github.com/qq940500529/secretbridge/pull/28)。

### 0.1.0-alpha.28 · SSH 远程命令（已完成本地验收）

目标：在 Web 和 AI 接口中完整使用 SSH 凭据代用，而不是仅提供一个连接探测函数。

- 固定连接地址、端口、用户名与可信 SHA256 指纹，不自动信任首次连接；
- 密码、Ed25519/ECDSA 私钥与独立解密口令协议认证，不生成临时私钥文件；
- 固定远程程序与逐项 POSIX 引用的普通参数，审批时解析预览并冻结；
- 共用授权、版本、轮换、幂等、并发、超时、取消和游标结果；
- 真实远程退出码、stdout/stderr 分流脱敏，以及取消后真实运输连接关闭；
- Web 配置到运行、原生 MCP/IPC 调用和 SQLite 重开端到端测试。

支持范围与限制见[SSH 凭据任务](docs/SSH凭据任务.md)。当前不支持 RSA、主机证书、Agent、PTY 或 Windows 原生远程 Shell；断开连接不等于远程业务撤回。

本地验收：Rust 103 项通过、2 项显式跳过，Web 54 项通过，仓库工具 20 项通过；Rust 格式/Clippy、Web 类型检查/构建、仓库与依赖许可检查通过（443 个 Rust 包、5 组 npm 许可）。真实 SSH 服务覆盖 Web 和原生 MCP/IPC 执行，私钥通过 Web 写入“SSH 密钥”引用后完成明文与加密认证。后台日志在全局 trace 下仍禁止协议库原始内容进入 stderr。实现与三平台检查见[PR #29](https://github.com/qq940500529/secretbridge/pull/29)，状态以实际 CI 为准。

原先 SSH 与 Git 的整体连接器计划拆为两个独立验收包：本版本交付 SSH 远程命令完整链路，下一版本继续文件传输与 Git，不将这两项算入已交付功能。

### 0.1.0-alpha.29 · 文件传输与 Git 连接器（已完成本地验收）

目标：实现日常远程维护和代码仓库的凭据代用。

- SSH：复用已完成的认证与主机校验，补充固定路径文件传输、覆盖确认、部分文件清理和传输结果；
- Git：HTTPS 令牌或 SSH 身份的拉取、推送和远端检查；
- 独立输入 schema、超时、取消、错误映射、脱敏和临时认证材料清理；
- 验收：真实本机 SSH 服务和本机 Git 远端完成认证、操作、失败及取消测试。

已交付 SFTP 单个普通文件分片上传／下载、默认覆盖拒绝、POSIX 扩展原子上传替换、同目录本机下载提交、1 GiB 最大配置上限、固定错误代码及取消／超时后最多额外 2 秒清理。复用既有 SSH 密码、私钥认证和主机指纹。清理失败显式返回 `cleanup_ok:false`，不会以先删除原目标来模拟原子替换。

Git 本版本选择 HTTPS 令牌路线，交付固定 URL／分支的远端查询、真实新增提交拉取与非强制推送。依赖用户安装的 Git，不引入 libgit2 或捆绑 Git；认证头只通过本次子进程环境交给 URL 范围内的配置，已知原令牌及认证编码表示均进入输出过滤。不写凭据 URL、argv、临时认证文件或凭据助手。拉取不自动合并工作区，取消不能撤回已经接受的推送。

独立配置界面、审批预览、Web/MCP 执行、幂等去重和 SQLite 重开均已接入。当前不含 Git SSH、clone、merge、force-push、递归文件传输或断点续传；Git SSH 作为后续扩展，不阻塞数据库查询与工作台开发。说明见[文件传输与 Git 任务](docs/文件传输与Git任务.md)。

本地验收：Rust 116 项通过、2 项显式跳过（系统凭据库写入和外部 TLS PostgreSQL），Web 59 项通过，仓库工具 20 项通过；Rust 格式/Clippy、Web 类型检查/构建、仓库和锁定依赖许可检查通过（448 个 Rust 包、5 组 npm 许可）。使用带认证的本机 Git smart-HTTP 远端、真实 SSH/SFTP 服务，覆盖成功、失败、原目标保留、超时／取消、清理失败、重定向拒绝、Web/原生 MCP/IPC 与配置重开。无窗口 Edge 浏览器交互冒烟通过，检查了 SFTP/Git 表单填写、执行方式切换和实际提交配置；该界面测试使用模拟 API，不代表业务连接验证。三平台验收以本版本 CI 为准，不将本机测试等同于业务环境验证。

2026-09-19，[PR #30](https://github.com/qq940500529/secretbridge/pull/30) 已通过三平台完整 CI 并合并，功能提交 `67e8310`，主分支合并提交 `b184a82`。

### 0.1.0-alpha.30 · 数据库查询连接器（已完成本地验收）

目标：覆盖最常见的凭据代用场景，而不是让用户全部依赖自由命令。

- PostgreSQL/MySQL：连接检查、版本信息和显式登记的只读查询模板；
- 每个连接器使用独立输入 schema、超时、取消、错误映射和脱敏规则；
- 验收：每类连接器至少有一个本机或容器化真实服务端到端测试。

已实现 PostgreSQL/MySQL 原生认证、连接检查、版本查询及固定 SELECT/WITH 模板。普通文本／整数／布尔值使用驱动绑定，不插值拼接 SQL；占位词法扫描排除引号、注释和 PostgreSQL dollar quote。派生 SELECT 显式投影登记列，数据库只读事务拒绝测试函数内的普通表写入。

每个模板支持 TLS 完整验证与私有 CA；明文仅限数值回环地址。结果限制 1—1000 行及约 192 KiB 预算，显式截断、文本／NULL 保留整数和小数精度；Web 表格、审批预览、MCP 原生 IPC、幂等和 SQLite 重开均已接入。PostgreSQL 原生取消请求、MySQL 按自身连接 ID 请求关闭与连接清理最多额外约 2 秒，失败显式标记；不将只读事务当作函数／扩展沙箱。

新增 MySQL 最小 Rust/rustls/ring 依赖与许可记录。真实 Windows 临时 PostgreSQL 17.11 和 MySQL 8.4.11 已完成认证、绑定、列范围、函数写入拒绝、超时／取消、私有 CA／主机名验证、Web/MCP 端到端验收。Docker 脚本和独立 CI 作业提供可重复的真实服务验收；三平台结果以本版本 CI 为准。当前不包含 MariaDB、Oracle/SQL Server、自由 SQL、数据库持久事务、写入模板、分页或导出。使用与测试见[数据库凭据任务](docs/数据库凭据任务.md)。

本地验收：常规 Rust 121 项通过，另 10 项新数据库真实服务测试显式运行并全部通过；常规回归中的 12 项跳过由这 10 项服务测试和原有 2 项外部／系统凭据库测试组成。Web 64 项、仓库工具 20 项通过；Rust 格式／Clippy、Web 类型检查／构建、仓库和许可检查通过（465 个 Rust 包、5 组 npm 许可）。无窗口 Edge 浏览器完成 SFTP/Git/数据库表单填写与提交检查；该 UI 脚本使用模拟 API，不代表业务服务验收。

2026-09-19，[PR #31](https://github.com/qq940500529/secretbridge/pull/31) 已通过 Windows/Linux/macOS 全部 CI 和独立 PostgreSQL/MySQL Docker 真实服务验收，并合并到主分支 `303320d`。

### 0.1.0-alpha.31 · 工作台体验重构（已完成本地验收）

目标：把现有管理页面整理成符合实际操作顺序的成熟工作台。

- 一级结构调整为“凭据、连接、任务、终端、历史、设置”；
- 新建和编辑使用抽屉或独立页面，列表与详情使用主从布局；
- 连接详情直接展示关联凭据、可用任务和最近执行；
- 任务运行时集中显示等待确认、执行中、需要输入、完成和失败；
- 全面检查键盘操作、窄屏、200%缩放、中英文长文本和错误恢复；
- 验收：新用户可仅依靠界面完成“保存凭据—建立连接—创建任务—授权—执行—查看结果”。

已实现六个一级入口；任务下分模板、授权确认、执行与结果，历史分执行和固定事件。凭据、连接、模板、授权、运行改为带搜索的主从布局，新建/编辑/申请/启动使用原生模态弹窗。连接详情列出任务实际使用的凭据、可用任务和最近五次执行，可带连接或任务上下文进入配置及授权。运行自动展开结果；过期及非当前任务授权不可选；设置接入现有页面会话撤销，不停止代理或持久终端。手机采用底部导航，中英文和长名称可换行，不添加应用依赖或数据库迁移。

本地验收：Web 75 项通过（含 8 项新增工作台测试和 3 个新页面文案检查）；Rust 常规 121 项通过、12 项按原有条件跳过，仓库工具 20 项通过。两组无窗口 Edge 脚本分别完成原有 SFTP/Git/数据库表单提交与完整模拟凭据/连接/任务/人工批准/执行/结果工作流，覆盖可重试保存失败、焦点进入/返回、搜索、语言切换和 720/390/320 像素逻辑宽度。真实业务执行由既有协议测试与 CI 验证；模拟 UI 不等同于业务服务或非开发者可用性观察。说明见[工作台使用说明](docs/工作台使用说明.md)。

2026-09-19，[PR #32](https://github.com/qq940500529/secretbridge/pull/32) 已通过全部 CI 并合并；主分支合并提交 `5630a3c`。

### 0.1.0-alpha.32 · 配置迁移、备份与诊断（已完成本地验收）

目标：让长期使用的数据可以迁移、备份和排查，而不导出秘密。

- 导入/导出非秘密配置、模板和连接定义；
- SQLite 一致性备份、恢复预检和版本迁移回滚；
- 凭据只导出引用和缺失状态，迁移后由用户在新设备重新录入；
- 生成默认脱敏的诊断包，包括版本、平台、状态和错误代码；
- 验收：全新目录恢复配置后能够重新绑定凭据并运行原任务。

已实现设置页三条独立工作流：配置导出、导入预检与人工确认；SQLite 一致性备份下载与只读恢复预检；固定结构精简诊断。配置导入追加新 UUID 并重映射所有连接和插槽，失败不留下部分记录；schema 15 持久化摘要回执，重复请求与重开后的重试不重复新增。备份通过原生 SQLite API 合入已提交 WAL 页面，输出独立文件，不导出系统凭据库秘密值。普通配置字段和历史仍可能包含业务信息，文件需要私密保存。

维护命令不启动 Web 服务：检查支持 schema 14—15 的完整备份，仅恢复到尚不存在的绝对目录，不覆盖原目录。恢复重置凭据配置标记、撤销待确认/已批准授权、将未完成运行标记中断；原目录保留用于回退。旧 schema 升级前保留一致性副本，失败时恢复原快照。诊断仅输出版本、平台、状态数量和白名单错误代码，不包括地址、路径、名称、参数或输出。既有 rusqlite 增加 backup/serialize 功能，没有新增应用依赖。

本地验收：Rust 135 项通过、12 项按既有外部条件跳过，Web 81 项及仓库工具 20 项通过；格式、Clippy、类型检查、构建、仓库和依赖许可检查通过。新增 14 项后端维护/CLI 测试，完成运行中 WAL 备份、全新目录恢复、页面重新录入秘密值、新授权、真实本机凭据命令执行与过滤结果的完整闭环。三组无窗口 Edge 脚本完成既有连接器表单、工作台全流程和维护页面模拟 API 工作流；维护页覆盖下载、失败后原文件重试/回执、中英文及 720/390/320 像素布局。真实数据库服务和三平台结果以本版本 CI 为准。使用与工程边界见[配置迁移与备份恢复](docs/配置迁移与备份恢复.md)。

## Beta：开源版跨平台交付与集中加固

Beta 阶段面向个人本机用户完成公开发行准备。公开发行仍要求跨平台构建、真实功能验证、依赖治理、可验证发行物和清楚文档；组织身份、集中审批、合规平台等企业能力不进入本阶段。

2026-09-19，[PR #33](https://github.com/qq940500529/secretbridge/pull/33) 已合并；配置维护功能进入主分支 `ca98c9c`。

### 0.2.0-beta.1 · 后台运行与安装包

- Windows、Linux、macOS 的后台启动、停止、开机启动和卸载；
- 安装、升级、失败回滚和数据保留选择；
- 可执行文件、Web 资源和迁移版本保持一致；
- 验收：三平台全新安装、覆盖升级、重启恢复与卸载通过。

本功能包实现原生后台控制、Web 确认停止、清单校验与不可变版本安装、激活失败恢复、上一版本回退、当前用户登录启动项及两种卸载数据选择；配套源码/许可打包工具与三平台隔离进程验收。没有新增应用依赖或数据库迁移。

安装包当前采用原生 ZIP/tar.gz，不含桌面壳、系统服务、安装向导或崩溃自动重启。卸载始终保留系统凭据库条目，未知文件不删除。版本回退不撤销数据库迁移；支持的包 schema 为 15。说明见[后台运行与安装交付](docs/后台运行与安装交付.md)。

**本阶段仍待完成的验收**：在 Windows 11 x64、Ubuntu 24.04 x64 和 macOS 14+ Apple Silicon 各完成一次真实桌面账号冒烟流程，覆盖首次安装、浏览器配对、保存测试凭据、执行获准任务、注销或重启后的恢复、升级和卸载。其他版本与架构先标记为实验性兼容。进程级自动化测试不代替这三次检查；SBOM 和依赖集中检查按 beta.2 推进。

本地验收已通过 Windows 原生包完整生命周期：后台分离、重复安装、损坏包拒绝、激活失败恢复、升级/回退、配置重开、自启动文件开关及默认保留/显式删除配置两种卸载。Web 84 项、仓库工具 25 项通过；Rust 常规回归、格式/Clippy、类型检查、构建、仓库及锁定依赖许可检查通过（465 个 Rust 包、5 组 npm 许可）。三组无窗口 Edge 脚本覆盖新停止工作流、原工作台和配置维护；新停止工作流检查双语、确认/取消、错误重试、离线状态及 1440/390/320 像素布局，使用模拟 API，不代表真实桌面登录验收。三平台结果以本版本 CI 为准。

2026-09-19，[PR #34](https://github.com/qq940500529/secretbridge/pull/34) 已通过 Windows、Linux、macOS 原生包生命周期及真实 PostgreSQL/MySQL 连接器检查并合并，主分支提交为 `4916842`。真实桌面首次安装、注销或整机重启、系统凭据库交互及浏览器配对仍保留为精简人工验收项。

### 0.2.0-beta.2 · 安全与可靠性集中验收

- 检查秘密回显、分片脱敏、临时文件、进程环境、命令历史和诊断包；
- 验证会话越权、审批绕过、重放、取消、服务崩溃和不确定结果；
- 完成三平台凭据库、IPC 文件权限和安装目录权限测试；
- 进行依赖漏洞、许可证、SBOM、校验值和确定性打包检查；
- 通过自动化真实服务与故障注入验证 DNS 超时、断网、服务重启、输出洪泛和清理失败；
- 修复阻断问题，但不把测试证据建设成面向普通用户的业务模块。

发行物可验证性功能包已经实现：`verify-package` 在安装前离线核验清单、全部文件摘要、Web/schema 元数据及 CycloneDX SBOM，且不执行待校验包内容；原生程序版本由可信构建与激活路径核验。打包器使用提交时间或 `SOURCE_DATE_EPOCH` 规范化 ZIP、tar.gz 和源码快照元数据，同一编译输入的两次打包必须逐字节一致。三平台原生验收包含 SBOM 篡改拒绝，发布工作流保留两次独立打包的摘要比较。

依赖漏洞门槛已按 [Issue #39](https://github.com/qq940500529/secretbridge/issues/39) 实现：普通 PR、主分支和原生包发布使用精确版本的 RustSec 与 pnpm 扫描器，公告源或报告异常时失败关闭；误报与临时接受项必须精确匹配包和公告、说明范围与理由，并在最多 90 天内到期。门槛首次执行发现 `RUSTSEC-2026-0285`，锁文件中的 rustls 已从 0.23.44 升级到官方修复版 0.23.45，没有增加例外。本机验证包含 35 项仓库工具测试、84 项 Web 测试、145 项 Rust 测试及 12 项外部条件跳过，并通过许可证、Clippy、真实公告扫描、Windows 双打包逐字节比较和隔离发行生命周期。

凭据泄漏集中验收已按 [Issue #37](https://github.com/qq940500529/secretbridge/issues/37) 开始推进：自动化使用唯一合成 canary 覆盖四种固定程序注入方式，并扫描正常 API、运行输出、历史事件、配置导出、诊断和 SQLite 备份；临时文件路径增加主动取消和超时清理验证。Windows CI 另用一次性条目验证 Credential Manager 写入、读取和删除，不接触用户已有凭据。详细边界与执行方式见[自动化安全验收](docs/自动化安全验收.md)。

开源版 1.0 不以不同构建机器重新编译所得二进制逐字节一致为发布条件，也不要求 Windows 代码签名、macOS 公证或 Linux 软件源签名。发行包保持未签名状态，并通过文档、版本清单、校验值、SBOM 和离线验证工具说明来源与完整性。

### 0.2.0-beta.3 · 稳定性与兼容性收口

- 完善已交付连接器的契约测试、错误语义和取消行为；（已完成）
- 长时间运行、输出洪泛、并发会话、网络抖动和磁盘异常测试；（已完成）
- 以 Chromium 为主要浏览器路径，补充 Firefox 和 Safari 的关键流程兼容检查；
- 完成键盘、缩放、窄屏和基础可访问性检查，不以企业级无障碍认证作为发布条件；
- 建立符合个人本机使用规模的启动、交互、长输出和资源占用基线；
- 冻结首个稳定版正式支持的平台、连接器和配置格式，其他环境明确标记为实验性。

连接器与终端稳定性验收已按 [Issue #40](https://github.com/qq940500529/secretbridge/issues/40) 实现。HTTP、SSH/SFTP、Git 与 PostgreSQL/MySQL 具有拒绝连接、响应或传输中断、超时、取消及清理失败的固定结果；真实数据库容器会在查询期间重启，并在失败收口后验证重新连接。八个并发任务会饱和四任务容量，验证排队与运行任务全部取消后许可归还，期间配置操作仍可响应并能继续执行后续任务。

凭据文件目录故障和 SQLite 模板事务中途失败用于验证无部分秘密文件、部分配置、失效关联或虚假成功。终端验收保留 2 MiB 输出洪泛、游标缺口、背压通知、重连与取消；`tools/test_stability_acceptance.py` 提供快速和重复长稳模式，Ubuntu 每周运行有界长稳工作流。命令、矩阵、固定错误与人工边界见[稳定性与故障注入验收](docs/稳定性与故障注入验收.md)。

## 1.0 发布条件

- 用户可以稳定完成凭据保存、真实终端、凭据任务、流式结果、取消和历史查看；
- Windows 11 x64、Ubuntu 24.04 x64 和 macOS 14+ Apple Silicon 有可安装、升级、回滚和卸载的发行包；
- AI 接口没有读取、导出或枚举秘密原文的能力；
- 已知秘密不会出现在正常输出、日志、历史、诊断包或 MCP 响应中；
- 核心连接器具有真实服务端测试和清楚的支持范围；
- 每个正式支持平台完成一次真实桌面冒烟验收，其余验证尽可能自动化；
- 文档、许可证、第三方声明、SBOM、校验值、版本和构建产物一致；
- 未签名发行包的系统提示、安装方法和完整性验证方式有清楚说明。

### 1.0 人工验收边界

人工验收只保留自动化无法可靠替代的真实系统交互：

- 三个正式支持平台各完成一次安装、浏览器配对、测试凭据保存、任务执行、重启恢复、升级和卸载；
- 检查 Credential Manager、Secret Service 和 Keychain 的真实授权、锁定或不可用状态；
- 用户对核心工作流进行一次主观可用性确认；
- 真实业务系统连接仅在用户提供授权和自行录入凭据时按需验证。

不要求正式招募测试人员、制作合规证据包或对全部浏览器、处理器架构和 Linux 发行版进行组合验收。

## 1.0 之后的开源版方向

- 根据真实个人用户需求增加连接器和任务模板；
- 评估稳定、受约束的连接器插件接口；
- 持续改善浏览器兼容、可访问性、性能和安装体验；
- 扩展经过真实设备验证的平台版本与处理器架构；
- 保持配置迁移、发行物验证和向后兼容策略。

这些方向按用户价值和维护成本排期，不为企业版能力预先建设复杂基础设施。

## 企业版独立规划

未来闭源商业企业版面向团队与组织客户，计划承载组织身份、角色权限、集中凭据与策略、多人审批、受管执行节点、集中审计、批量部署、高可用和商业支持。企业版单独规划、单独交付，不计入本路线图的未完成任务。

完整差异和代码边界见[开源版与企业版产品边界](docs/开源版与企业版.md)。

## 不进入开源版近期范围

- 多租户云端凭据中心；
- 团队审批、组织策略和集中审计平台；
- 试点活动管理、认证证据中心或合规工作流；
- 企业设备管理、批量部署、高可用与集中灾备；
- AI 自主批准高风险任务；
- 向 AI 返回秘密、通用密码导出或无限制带密 Shell；
- 远程控制面和受管代理集群；
- 桌面壳及仅为商业差异而设置的本机功能限制。

---

[开源版与企业版产品边界](docs/开源版与企业版.md) · [成熟态软件架构与功能说明](docs/成熟态软件架构与功能说明.md) · [开发设计](docs/开发设计.md) · [安全模型](docs/安全模型与验收.md) · [变更记录](CHANGELOG.md)
