# SecretBridge 功能优先路线图

[项目首页](README.zh-CN.md) / [文档中心](docs/README.md) / [成熟态架构](docs/成熟态软件架构与功能说明.md)

SecretBridge 的目标很直接：用户在本机保存凭据，AI 不读取凭据原文，但可以通过受控的本地代理完成真正的终端、数据库、SSH 和 HTTP 任务，并持续获得脱敏结果。

路线图从 `0.1.0-alpha.22` 起按“先可用、后完善、发布前集中加固”的顺序推进。安全是执行底座的约束，不再单独扩展成试点、认证或证据管理产品。

## 开发原则

1. 每个版本交付一个可在界面和 AI 接口中端到端使用的大功能包。
2. 优先完成真实终端、凭据注入、任务模板和常用连接器，不用合成能力代替真实功能。
3. 必要安全控制随功能同时实现：秘密不回读、本机绑定、权限校验、输出脱敏、临时材料清理和失败状态。
4. 高强度攻击测试、安装身份隔离、供应链审查和发行签名在功能基本完整后集中进行。
5. 开发阶段、里程碑编号和安全宣传不进入运行界面；界面只表达用户任务、状态和下一步操作。
6. 不通过“通用脚本加秘密”绕过产品边界。每种注入方式和连接器都必须明确输入、输出、生命周期与清理规则。

## 当前基线：0.1.0-alpha.25

### 已保留的产品底座

- 本机 Web 控制台、一次性浏览器配对、可撤销页面会话和精确 Origin 校验；
- SQLite 配置目录与操作系统凭据库，秘密只有写入、更新和删除路径，没有读取或导出 API；
- 凭据引用、目标、操作模板、限时审批、单次运行和固定审计事件；
- 后台代理与 MCP stdio 桥接分离，使用 Windows 命名管道或 Unix domain socket 通信；
- 可重连的真实 PTY：Windows PowerShell／CMD、Linux Bash、macOS Zsh，支持名称、工作目录、普通环境、PID、退出码、输入租约、游标回放和输出背压；
- MCP 普通终端控制、独立会话输入权、长轮询增量读取，以及 Web 审批／任务／终端变更实时通知；
- 固定程序凭据任务、四种命名插槽注入方式、跨分片脱敏输出与 MCP 续读；
- PostgreSQL 固定只读连接检查，以及平台信任根或显式私有 CA 支持。

### 本次删除的超前产品层

- 安全验证与证据中心；
- 平台身份与 IPC 证据工作区；
- 试点准入快照；
- 低权限试点活动、固定场景矩阵和相关报告；
- 上述能力对应的页面、API、领域模型、测试和数据库表。

旧版数据库升级到 schema v12 时会删除这些废弃表，同时保留凭据引用、目标、模板、审批、运行和审计数据。

## 近期开发：把核心工作流做完整

### 0.1.0-alpha.23 · 真实持久终端（已完成）

目标：用真正的系统终端替代当前合成 PTY，让用户可以在 Web 界面完成日常命令行工作。

- Windows 支持 PowerShell 和 CMD；Linux 支持 Bash；macOS 支持 Zsh；
- 创建、命名、列出、连接、断开、重连、调整大小和终止会话；
- 保持工作目录和普通环境状态，支持长任务与流式 stdout/stderr；
- 区分窗口断开、会话退出和强制终止；
- 支持输入租约，避免 Web 用户和 AI 同时写入；
- 验收：三个平台至少各完成一次真实 shell 会话，关闭浏览器后可重新连接，长输出与取消行为明确。

实现已包含平台能力探测、白名单Shell解析、Web创建表单、显式窗口断开、自动／手动重连、进程退出和强制终止语义，并由三平台CI运行真实Shell集成测试。测试专用合成PTY只用于稳定复现输出洪泛、游标截断和撤销场景，不再出现在正式界面。

### 0.1.0-alpha.24 · AI 终端控制与即时状态（已完成）

目标：让 AI 通过本机 MCP 操作真实终端，并及时获得增量结果。

- 新增终端列表、创建、连接、写入、增量读取、调整尺寸、取消和关闭工具；
- 输出使用单调游标续读，断线后不重复执行命令；
- WebSocket 向 Web 控制台实时推送会话、任务和审批变化；
- MCP 返回终端状态、Shell退出码和有界的普通进程输出，不返回内部认证令牌；普通终端不注入凭据，凭据输出脱敏由下一功能包提供；
- 验收：AI 可完成“创建终端—执行普通命令—持续读取—结束会话”的完整闭环。

实现包含10个终端工具，复用现有真实PTY和原生IPC；MCP客户端之间及Web页面通过输入租约协调。正常MCP断开释放输入权，异常退出的附件在60秒空闲期限后清理，终端进程保留。读取最多16KiB字节、最多等待5秒，提供单调游标、保留缺口、原始字节及UTF-8预览；写入失败不自动重试。Web变更流只推送失效通知，不承载资源值或终端输出，页面重连后重新获取快照，并保留低频兜底刷新。

2026-09-17，功能提交 `44ce15d` 已通过 Windows、Linux 和 macOS 的全套 CI，包含真实 Shell 的 MCP／原生 IPC 端到端验收、Web 测试、Rust 检查和依赖许可检查。实现见 [PR #25](https://github.com/qq940500529/secretbridge/pull/25)。

### 0.1.0-alpha.25 · 凭据注入与跨分片脱敏（已实现，待三平台 CI 验收）

目标：让 AI 能使用凭据执行任务，同时不通过正常工具接口取得凭据原文。

- 在 UI 中把凭据绑定到任务模板的命名插槽；AI 只看到插槽名称和用途；
- 支持标准输入、临时环境变量、命令参数和短期配置文件四种注入方式；
- UI 明确显示每种方式的暴露风险，默认优先标准输入或目标工具的原生认证机制；
- 注入值不写入命令历史、任务定义、SQLite、普通终端环境或 MCP 请求；
- stdout、stderr、错误信息、审计摘要和实时分片使用同一跨分片脱敏器；
- 任务结束、取消和服务异常恢复时清除临时文件与可清理内存；
- 验收：使用合成秘密覆盖分片、编码、回显、取消和异常路径，AI 只获得脱敏结果。

已实现固定程序模板、最多8个命名凭据插槽、四种注入方式、显式脱敏输出范围、审批详情配置预览、单次授权与幂等运行、四进程并发限制、超时／取消、固定退出状态、临时文件清理与启动恢复清理。stdout/stderr在解码、保存和返回前执行字节级过滤，增量UTF-8解码避免中文跨分片损坏；Web和MCP共享有界分片游标输出。schema v13保存配置及已过滤输出，凭据轮换使关联模板版本递增。当前不提供交互式密码提示，也不承诺识别任意秘密变换或隔离恶意程序；详见[凭据命令任务](docs/凭据命令任务.md)。

### 0.1.0-alpha.26 · 可复用任务模板

目标：把常用操作从手工拼命令升级为可保存、可审阅、可复用的任务。

- 模板包含固定程序、参数结构、工作目录、普通参数、凭据插槽、注入方式、超时和结果规则；
- 普通参数有类型、必填、默认值、枚举和长度约束；
- 支持“每次确认”“一段时间内允许”和“仅当前一次”三种用户授权方式；
- 模板变更后旧授权失效；重复请求使用幂等键避免重复执行；
- 任务详情展示实时输出、最终状态、退出码、耗时和可重试建议；
- 验收：从 UI 创建模板后，Web 与 MCP 均可使用同一执行链完成任务。

### 0.1.0-alpha.27 · 常用连接器包

目标：覆盖最常见的凭据代用场景，而不是让用户全部依赖自由命令。

- SSH：主机密钥确认、密码或密钥认证、固定命令和文件传输；
- HTTP：固定方法、URL 模板、Header/Body 凭据插槽和状态/字段筛选；
- PostgreSQL/MySQL：连接检查、版本信息和显式登记的只读查询模板；
- Git：HTTPS 令牌或 SSH 身份的拉取、推送和远端检查；
- 每个连接器使用独立输入 schema、超时、取消、错误映射和脱敏规则；
- 验收：每类连接器至少有一个本机或容器化真实服务端到端测试。

### 0.1.0-alpha.28 · 工作台体验重构

目标：把现有管理页面整理成符合实际操作顺序的成熟工作台。

- 一级结构调整为“凭据、连接、任务、终端、历史、设置”；
- 新建和编辑使用抽屉或独立页面，列表与详情使用主从布局；
- 连接详情直接展示关联凭据、可用任务和最近执行；
- 任务运行时集中显示等待确认、执行中、需要输入、完成和失败；
- 全面检查键盘操作、窄屏、200%缩放、中英文长文本和错误恢复；
- 验收：新用户可仅依靠界面完成“保存凭据—建立连接—创建任务—授权—执行—查看结果”。

### 0.1.0-alpha.29 · 配置迁移、备份与诊断

目标：让长期使用的数据可以迁移、备份和排查，而不导出秘密。

- 导入/导出非秘密配置、模板和连接定义；
- SQLite 一致性备份、恢复预检和版本迁移回滚；
- 凭据只导出引用和缺失状态，迁移后由用户在新设备重新录入；
- 生成默认脱敏的诊断包，包括版本、平台、状态和错误代码；
- 验收：全新目录恢复配置后能够重新绑定凭据并运行原任务。

## Beta：跨平台交付与集中加固

### 0.2.0-beta.1 · 后台运行与安装包

- Windows、Linux、macOS 的后台启动、停止、开机启动和卸载；
- 安装、升级、失败回滚和数据保留选择；
- 可执行文件、Web 资源和迁移版本保持一致；
- 验收：三平台全新安装、覆盖升级、重启恢复与卸载通过。

### 0.2.0-beta.2 · 安全与可靠性集中验收

- 检查秘密回显、分片脱敏、临时文件、进程环境、命令历史和诊断包；
- 验证会话越权、审批绕过、重放、取消、服务崩溃和不确定结果；
- 完成三平台凭据库、IPC 文件权限和安装目录权限测试；
- 进行依赖漏洞、许可证、SBOM、构建复现和发布签名检查；
- 修复阻断问题，但不把测试证据建设成面向普通用户的业务模块。

### 0.2.0-beta.3 · 扩展与稳定性

- 连接器契约测试与插件接口；
- 长时间运行、输出洪泛、并发会话、网络抖动和磁盘异常测试；
- 性能、资源占用、浏览器兼容和可访问性优化；
- 冻结首个稳定版支持的平台、连接器和配置格式。

## 1.0 发布条件

- 用户可以稳定完成凭据保存、真实终端、凭据任务、流式结果、取消和历史查看；
- Windows、Linux、macOS 有可安装、升级、回滚和卸载的发行包；
- AI 接口没有读取、导出或枚举秘密原文的能力；
- 已知秘密不会出现在正常输出、日志、历史、诊断包或 MCP 响应中；
- 核心连接器具有真实服务端测试和清楚的支持范围；
- 文档、许可证、第三方声明、SBOM、版本和构建产物一致。

## 暂不进入近期范围

- 多租户云端凭据中心；
- 团队审批、组织策略和集中审计平台；
- 试点活动管理、认证证据中心或合规工作流；
- AI 自主批准高风险任务；
- 向 AI 返回秘密、通用密码导出或无限制带密 Shell；
- 在核心单机功能成熟前建设远程控制面。

---

[成熟态软件架构与功能说明](docs/成熟态软件架构与功能说明.md) · [开发设计](docs/开发设计.md) · [安全模型](docs/安全模型与验收.md) · [变更记录](CHANGELOG.md)
